import * as React from 'react';
export declare type ReactComponent<Props = {}> = React.ComponentClass<Props> | React.StatelessComponent<Props> | typeof React.PureComponent;
export interface State {
    isLoading: boolean;
    error: Error | null;
}
export declare type StateWithData<Data> = State & Readonly<{
    data: Data | null;
}>;
