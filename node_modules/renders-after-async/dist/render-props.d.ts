import * as React from 'react';
import { StateWithData } from './types';
interface Props<Data> extends Readonly<{
    children: (props: StateWithData<Data>) => JSX.Element | null;
    performAsync: () => Promise<Data>;
}> {
}
export declare class RendersAfterAsync<Data> extends React.Component<Props<Data>, StateWithData<Data>> {
    state: {
        data: null;
        error: null;
        isLoading: boolean;
    };
    private hasBeenCancelled;
    componentDidMount(): Promise<void>;
    componentWillUnmount(): void;
    render(): JSX.Element | null;
}
export {};
